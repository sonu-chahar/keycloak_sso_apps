package com.keycloak.validator;

//@Component
public class UserValidator/* implements Validator */ {

	/*
	 * @Resource private UserMasterService userService;
	 * 
	 * @Override public boolean supports(Class<?> aClass) { return
	 * UserMaster.class.equals(aClass); }
	 * 
	 * @Override public void validate(Object o, Errors errors) { UserMaster user =
	 * (UserMaster) o;
	 * 
	 * ValidationUtils.rejectIfEmptyOrWhitespace(errors, "username", "NotEmpty"); if
	 * (user.getUsername().length() < 6 || user.getUsername().length() > 32) {
	 * errors.rejectValue("username", "Size.userForm.username"); } if
	 * (userService.findByUsername(user.getUsername()) != null) {
	 * errors.rejectValue("username", "Duplicate.userForm.username"); }
	 * 
	 * ValidationUtils.rejectIfEmptyOrWhitespace(errors, "password", "NotEmpty"); if
	 * (user.getPassword().length() < 8 || user.getPassword().length() > 32) {
	 * errors.rejectValue("password", "Size.userForm.password"); }
	 * 
	 * if (!user.getConfirmPassword().equals(user.getPassword())) {
	 * errors.rejectValue("passwordConfirm", "Diff.userForm.passwordConfirm"); } }
	 */
}
